version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "../../db/migrations"
    gen:
      go:
        package: "db"
        out: "generated"
        emit_json_tags: true
        emit_interface: true
        emit_exact_table_names: false